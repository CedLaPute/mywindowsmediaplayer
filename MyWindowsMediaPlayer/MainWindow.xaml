<Window x:Class="MyWindowsMediaPlayer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:MyWindowsMediaPlayer"
    Title="MopMopMop" Height="336" Width="728">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="101*"/>
            <ColumnDefinition Width="192*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <MediaElement x:Name="player" Width="0" Height="0" LoadedBehavior="Manual" Grid.Column="1" Margin="45.8,20,146.6,20" />

        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,-0.4,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="Audio" Grid.Column="0">Audio</Button>
            <Button x:Name="Video" Grid.Column="1">Video</Button>
            <Button x:Name="Picture" Grid.Column="2">Picture</Button>
        </Grid>
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,-0.4,0">
            <TextBlock HorizontalAlignment="Left" x:Name="Time" Text="00:00 / 00:00" />
            <TextBlock HorizontalAlignment="Left" x:Name="Title" Margin="100,0,0,0" Text="Current Song" />
        </Grid>

        <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,-0.4,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="button_back" Grid.Column="0">Back</Button>
            <Button x:Name="button_stop" Grid.Column="1">Stop</Button>
            <Button x:Name="button_play" Grid.Column="2">Play/Pause</Button>
            <Button x:Name="button_forward" Grid.Column="3">Forward</Button>
            <Button x:Name="button_add" Grid.Column="4">Add File</Button>
            <Button x:Name="button_repeat" Grid.Column="5">Repeat</Button>
            <Button x:Name="button_shuffle" Grid.Column="6">Shuffle</Button>
        </Grid>

        <ScrollViewer Grid.Row="3" Grid.ColumnSpan="2" Margin="0,0,-0.4,0">
            <ScrollViewer Grid.Row="3">
                <ListBox ItemsSource="{Binding Path=Datas}">
                    <!-- magic playlist -->
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </ScrollViewer>
    </Grid>
</Window>