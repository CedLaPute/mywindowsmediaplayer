<Window x:Class="MyWindowsMediaPlayer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:MyWindowsMediaPlayer"
    Title="MopMopMop" Height="700" Width="800" Background="Gray">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="101*"/>
            <ColumnDefinition Width="192*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="456"/>
            <RowDefinition Height="135.2"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <MediaElement x:Name="player" Height="440" LoadedBehavior="Manual" Grid.Row="1" Grid.ColumnSpan="2" Margin="27,10.2,26.6,0" VerticalAlignment="Top"/>

        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,-0.4,1.8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="Audio" Grid.Column="0">Audio</Button>
            <Button x:Name="Video" Grid.Column="1">Video</Button>
            <Button x:Name="Picture" Grid.Column="2">Picture</Button>
        </Grid>


        <Grid Grid.Row="3" Grid.ColumnSpan="2" Margin="0,0,-0.4,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="button_back" Grid.Column="0">Back</Button>
            <Button x:Name="button_stop" Grid.Column="1">Stop</Button>
            <Button x:Name="button_play" Grid.Column="2">Play/Pause</Button>
            <Button x:Name="button_forward" Grid.Column="3">Next element</Button>
            <Button x:Name="button_add" Grid.Column="4">Add File</Button>
        </Grid>

        <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,1.2,-0.4,0">
            <ScrollViewer Grid.Row="2">
                <ListBox ItemsSource="{Binding Path=Datas}">
                    <!-- magic playlist -->
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>